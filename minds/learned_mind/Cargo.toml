[package]
name = "learned_mind"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
blob_interface = { path = "../../blob_interface" }
blob_policy = { path = "../../blob_policy" }
extism-pdk = { version = "1.4.1", default-features = false }
